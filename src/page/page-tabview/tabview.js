$(function(){
	// function tabclick(tabObj) {
	// 	if ( tabObj.id == 'home' ) {
			
	// 	}
	// 	if ( tabObj.id == 'profile' ) {
			
	// 	}
	// }
	ReactDOM.render(
        <h1>Hello, test!</h1>,
        document.getElementById('test')
    );

	ReactDOM.render(
        <h1>Hello, world!</h1>,
        document.getElementById('home')
    );
	
});